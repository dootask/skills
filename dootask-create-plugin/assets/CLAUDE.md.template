# {{插件名}} — DooTask 插件

{{产品一句话描述}}。appid `{{appid}}`，{{Next.js / TanStack Start}} 单镜像全栈。

## 与主程序交互：先读本地，别联网

主程序、工具库、文档都在本机。查 DooTask API / 约定 / 主程序行为时**读本地源码，不要上网搜 DooTask**（线上文档会过时/不准）：

- 主程序源码：`/home/coder/workspaces/dootask`
- `@dootask/tools`（前端库 + Go/Node/Python SDK）：`/home/coder/workspaces/dootask-tools`
- 插件开发文档：`/home/coder/workspaces/dootask-appstore/appstore/apps/_/README_CN.md`
- 同栈样板：`/home/coder/workspaces/dootask-plugins/crm`（TanStack Start）、`.../asset-hub`（Next.js）

## 雷区（踩了就 404 / 装不上）

- **basePath 四处必须一致**，否则静态资源/路由 404，全是 `/apps/{{appid}}`：前端 base（`{{base 配置文件}}`）、`{{版本}}/nginx.conf` 的 `location`、`{{版本}}/config.yml` 的 `menu_items.url`、前端内部链接。
- 容器监听 **3000**、不对外暴露端口；nginx 上游写**显式端口** `http://{{appid}}:3000`（漏端口 → 502）。
- `appid` = 镜像名 `dootask/{{appid}}` = 路径前缀，三者同源别分叉。
- 顶层 `config.yml` 只放元数据；`fields`/`menu_items`/`hooks` 放 `{{版本}}/config.yml`。

## 命令

```bash
cd src && pnpm install && pnpm dev                                # 本地开发（脱离宿主自动降级）
docker build -t dootask/{{appid}}:{{版本}} -f src/Dockerfile src   # 构建：tag 必须 = 安装版本号
# 部署到测试目录：技能 dootask-create-plugin 的 scripts/deploy_to_test.sh <本目录> {{版本}} {{作者}}
#   → 拷 config.yml+logo*+README*+{{版本}}/ 到 apps/community_{{作者}}_{{appid}}/，排除 src/.build.yml/CLAUDE.md
```

装：DooTask 后台 → 应用商店 → 更新应用列表 → 安装。验证：`docker ps|grep {{appid}}`、`docker logs`、打开 `/apps/{{appid}}` 应显示当前用户名。

## 目录

`src/` 源码+Dockerfile；`{{版本}}/` 版本目录（config.yml/docker-compose.yml/nginx.conf/CHANGELOG）；`.build.yml` 构建配置。安装字段 `fields` → 环境变量，见 `{{版本}}/{config.yml,docker-compose.yml}`。
