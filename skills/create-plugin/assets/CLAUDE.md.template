# {{插件名}} — DooTask 插件

{{产品一句话描述}}。appid `{{appid}}`，{{Next.js / TanStack Start}} 单镜像全栈。

## 本地参考：先读本地，别联网

查 DooTask API / 约定 / 主程序行为时优先读本机源码，**别上网搜 DooTask**（线上文档会过时/不准）。下表代表词（`ref:xxx`）按「**本地路径存在就读 → 否则 `git clone --depth=1 https://github.com/<在线仓库> ${DOOTASK_REFS:-/tmp/dootask-refs}/<repo>` 后读 → 私有/拿不到则就现有信息谨慎处理**」获取：

| 代表词 | 用途 | 本机本地路径 | 在线仓库（clone 源） |
| --- | --- | --- | --- |
| `ref:dootask` | 主程序源码 | `/home/coder/workspaces/dootask` | `kuaifan/dootask`（公开） |
| `ref:tools` | `@dootask/tools` 前端库 + Go/Node/Python SDK | `/home/coder/workspaces/dootask-tools` | `dootask/tools`（公开） |
| `ref:appstore-docs` | 官方插件开发文档 `apps/_/README_CN.md` | `/home/coder/workspaces/dootask-appstore/appstore/apps/_/README_CN.md` | `dootask/appstore`（**私有**，需 gh 授权） |
| `ref:crm` | 同栈样板·TanStack Start | `/home/coder/workspaces/dootask-plugins/crm` | `dootask/crm`（公开） |
| `ref:asset-hub` | 同栈样板·Next.js | `/home/coder/workspaces/dootask-plugins/asset-hub` | `dootask/asset-hub`（公开） |

> 上表取自技能 `dootask:create-plugin` 的 `references/samples.md`（按本插件栈已裁剪）；用到表外的样板时从该文件补对应 `ref:` 行。

## 雷区（踩了就 404 / 装不上）

- **basePath 四处必须一致**，否则静态资源/路由 404，全是 `/apps/{{appid}}`：前端 base（`{{base 配置文件}}`）、`{{版本}}/nginx.conf` 的 `location`、`{{版本}}/config.yml` 的 `menu_items.url`、前端内部链接。
- 容器监听 **3000**、不对外暴露端口；nginx 上游写**显式端口** `http://{{appid}}:3000`（漏端口 → 502）。
- `appid` = 镜像名 `dootask/{{appid}}` = 路径前缀，三者同源别分叉。
- 顶层 `config.yml` 只放元数据；`fields`/`menu_items`/`hooks` 放 `{{版本}}/config.yml`。
- `@dootask/tools` 分两侧别混用：**前端侧**(`appReady`/`getUserInfo`，依赖 window，SSR 下必须动态 `import()`)、**后端侧**(`DooTaskClient`，调主程序 `http://nginx`)。
- **右上角被主程序胶囊占用**：自身 UI 别放右上角；按需 `setCapsuleConfig` 调显隐/位置、`addMenuClickListener` 收「更多」点击（详情读 `ref:tools`）。
- **接入主程序 AI（适合就留，纯底层插件可删此条）**：新增值得被调用的后端操作时同步维护 `openapi`（指向 OpenAPI/Swagger 规范）→ 用户/AI 可 `doo app call {{appid}} <指令>` 执行；新增面向用户的用法时补 `knowledge_base` 知识库目录 → 产品内「AI 助手」可解答。详见 `ref:appstore-docs`。

## 命令

```bash
cd src && pnpm install && pnpm dev                                # 本地开发（脱离宿主自动降级）
docker build -t dootask/{{appid}}:{{版本}} -f src/Dockerfile src   # 构建：tag 必须 = 安装版本号
# 上传+安装+验证全在 doo 内闭环（前置：sudo npm i -g @dootask/cli + doo auth login）：
# 1. 技能 dootask:create-plugin 的 scripts/upload_to_appstore.sh <本目录> {{版本}} {{作者}}
#                                                            # 打包 → tar.gz → doo app upload
# 2. doo app fields  community_{{作者}}_{{appid}}            # 看 fields（若有）
# 3. doo app install community_{{作者}}_{{appid}} [--param K=V ...]
# 4. doo app containers/logs community_{{作者}}_{{appid}}    # 验证
```

验证：`doo app containers community_{{作者}}_{{appid}}` 看容器、`doo app logs <id>` 看日志、打开 `/apps/{{appid}}` 应显示当前用户名。回滚：`doo app uninstall <id> [--delete-data]` / `doo app remove <id>`。

> `doo` 命令不确定时以 `doo --help` / `doo app --help` 为准（源码见 `ref:tools` 的 `server/cli`）。

## 目录

`src/` 源码+Dockerfile；`{{版本}}/` 版本目录（config.yml/docker-compose.yml/nginx.conf/CHANGELOG）；`.build.yml` 构建配置。安装字段 `fields` → 环境变量，见 `{{版本}}/{config.yml,docker-compose.yml}`。
